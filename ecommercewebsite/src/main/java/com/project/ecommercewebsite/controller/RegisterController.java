// package com.project.ecommercewebsite.controller;

// import org.springframework.http.ResponseEntity;

// public class RegisterController {
// @PostMapping("/signup")
// public ResponseEntity<AccountDto> createUser(@Valid @RequestBody AccountDto
// accountDto) {
// accountService.registerUser(accountDto);
// return ResponseEntity.ok().body(accountDto);
// }

// @PostMapping("/signup")
// public ResponseEntity<AccountDto> createAdmin(@Valid @RequestBody AccountDto
// accountDto) {
// accountService.registerAdmin(accountDto);
// return ResponseEntity.ok().body(accountDto);
// }
// }
